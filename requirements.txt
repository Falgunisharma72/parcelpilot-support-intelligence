# The model backend is pluggable. `openai` is the client for every
# OpenAI-compatible endpoint (Groq, Gemini, OpenRouter, Cerebras, Mistral,
# Together, Ollama) - which is how the whole system runs on a free tier.
# `anthropic` is only imported if you actually select that provider.
openai==3.3.1
anthropic==1.0.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pdfplumber==0.11.10
openpyxl==3.1.5
PyYAML==6.0.3
python-dotenv==1.2.3
